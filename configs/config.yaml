model:
  text_model: roberta-base
  image_model: resnet50
  embedding_dim: 512

training:
  batch_size: 4
  lr: 0.0001
  epochs: 5
  num_workers: 0
  device: cuda

data:
  max_length: 128
  image_size: 224

output:
  checkpoint_dir: outputs/checkpoints

