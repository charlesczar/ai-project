model:
  text_model: roberta-base
  image_model: resnet50
  embedding_dim: 512

training:
  batch_size: 16
  lr: 0.0001
  epochs: 10
  num_workers: 2
  device: cuda

data:
  max_length: 128
  image_size: 224

output:
  checkpoint_dir: outputs/checkpoints

